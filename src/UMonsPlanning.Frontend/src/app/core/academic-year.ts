/**
 * Displayed on the home page's intro paragraph. Updated once per academic year - the frontend
 * equivalent of `Pronote:BaseUrl` on the backend (CLAUDE.md §12), which also encodes the year and
 * is updated the same way, each rentrée.
 */
export const CURRENT_ACADEMIC_YEAR = '2026-2027';
